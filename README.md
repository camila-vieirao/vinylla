# SETUP DATABASE
1. run the script `dump.sql` located in the `vinylla/db` folder to create the necessary database schema.

# RUN BACKEND SERVER
```
cd backend
npm install
npm run dev
```
# RUN FRONTEND SERVER
```
cd frontend
npm install
npm run dev
```